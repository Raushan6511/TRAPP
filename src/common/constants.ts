export enum BUTTON_PRESET {
  CUSTOM = "custom",
  PRIMARY = "primary",
  SECONDARY = "secondary",
  WITH_ICON = "with_icon",
}

export enum INPUT_FIELD_PRESETS {
  DOB = "dob",
  NUMERIC = "numeric",
  PASSWORD = "password",
}
